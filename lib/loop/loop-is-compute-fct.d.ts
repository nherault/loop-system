import { IsComputeFct } from './loop-system.types';
/**
 * Compute once
 * @param delta
 * @param computeElement
 */
export declare const computeOnce: IsComputeFct;
/**
 * Compute using a time interval set on the "computeElement" object
 * @param delta
 * @param computeElement
 */
export declare const computeTimeInterval: IsComputeFct;
